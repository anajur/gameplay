import {
        REDIRECT_URI,
        SCOPE,
        RESPONSE_TYPE,
        CDN_IMAGE,
        CLIENT_ID
} from './discordAuth';

export {
    REDIRECT_URI,
    SCOPE,
    RESPONSE_TYPE,
    CDN_IMAGE,
    CLIENT_ID
}